import 'package:flutter/material.dart';

class PrediccionesDF extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Posibles Incidencias'),
      ),
      body: Center(
        child: Text('Aquí aparecerán las posibles incidencias.'),
      ),
    );
  }
}
